apply plugin: 'java'
apply plugin: 'maven'
apply plugin: 'application'

//outputDir = "../sdk/src/main/java"

mainClassName = "com.braunster.greendao.generator.Generator"

dependencies {
    compile 'de.greenrobot:DaoGenerator:1.3.0'
}

//task createDocs {
//    def docs = file(outputDir)
//    docs.mkdirs()
//}

run {
//    args outputDir
}

artifacts {
    archives jar
}