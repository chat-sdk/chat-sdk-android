package com.braunster.chatsdk.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import java.util.ArrayList;
import java.util.List;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table BLINK_DATA.
*/
public class BLinkDataDao extends AbstractDao<BLinkData, Long> {

    public static final String TABLENAME = "BLINK_DATA";

    /**
     * Properties of entity BLinkData.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property UserID = new Property(1, Long.class, "UserID", false, "USER_ID");
        public final static Property ThreadID = new Property(2, Long.class, "ThreadID", false, "THREAD_ID");
    };

    private DaoSession daoSession;

    private Query<BLinkData> bThread_BLinkDataQuery;
    private Query<BLinkData> bUser_BLinkDataQuery;

    public BLinkDataDao(DaoConfig config) {
        super(config);
    }
    
    public BLinkDataDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'BLINK_DATA' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'USER_ID' INTEGER," + // 1: UserID
                "'THREAD_ID' INTEGER);"); // 2: ThreadID
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'BLINK_DATA'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, BLinkData entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long UserID = entity.getUserID();
        if (UserID != null) {
            stmt.bindLong(2, UserID);
        }
 
        Long ThreadID = entity.getThreadID();
        if (ThreadID != null) {
            stmt.bindLong(3, ThreadID);
        }
    }

    @Override
    protected void attachEntity(BLinkData entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public BLinkData readEntity(Cursor cursor, int offset) {
        BLinkData entity = new BLinkData( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // UserID
            cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2) // ThreadID
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, BLinkData entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setUserID(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setThreadID(cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(BLinkData entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(BLinkData entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "BLinkData" to-many relationship of BThread. */
    public List<BLinkData> _queryBThread_BLinkData(Long ThreadID) {
        synchronized (this) {
            if (bThread_BLinkDataQuery == null) {
                QueryBuilder<BLinkData> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.ThreadID.eq(null));
                bThread_BLinkDataQuery = queryBuilder.build();
            }
        }
        Query<BLinkData> query = bThread_BLinkDataQuery.forCurrentThread();
        query.setParameter(0, ThreadID);
        return query.list();
    }

    /** Internal query to resolve the "BLinkData" to-many relationship of BUser. */
    public List<BLinkData> _queryBUser_BLinkData(Long UserID) {
        synchronized (this) {
            if (bUser_BLinkDataQuery == null) {
                QueryBuilder<BLinkData> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.UserID.eq(null));
                bUser_BLinkDataQuery = queryBuilder.build();
            }
        }
        Query<BLinkData> query = bUser_BLinkDataQuery.forCurrentThread();
        query.setParameter(0, UserID);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getBUserDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getBThreadDao().getAllColumns());
            builder.append(" FROM BLINK_DATA T");
            builder.append(" LEFT JOIN BUSER T0 ON T.'USER_ID'=T0.'_id'");
            builder.append(" LEFT JOIN BTHREAD T1 ON T.'THREAD_ID'=T1.'_id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected BLinkData loadCurrentDeep(Cursor cursor, boolean lock) {
        BLinkData entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        BUser bUser = loadCurrentOther(daoSession.getBUserDao(), cursor, offset);
        entity.setBUser(bUser);
        offset += daoSession.getBUserDao().getAllColumns().length;

        BThread bThread = loadCurrentOther(daoSession.getBThreadDao(), cursor, offset);
        entity.setBThread(bThread);

        return entity;    
    }

    public BLinkData loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<BLinkData> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<BLinkData> list = new ArrayList<BLinkData>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<BLinkData> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<BLinkData> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
