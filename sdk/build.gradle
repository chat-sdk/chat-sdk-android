apply plugin: 'android-library'
android {
    compileSdkVersion 19
    buildToolsVersion '19.1.0'
    defaultConfig {
        minSdkVersion Integer.parseInt(project.MIN_SDK)
        targetSdkVersion 19
        versionCode 1
        versionName '1.0'
    }
    buildTypes {
        release {
            runProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
    productFlavors {
    }
}
dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    //Google Play Services - For Google Maps
    compile 'com.google.android.gms:play-services:4.4.+'
    // Support Library.
    compile 'com.android.support:appcompat-v7:19.+'
    // Volley - Networking library from google.
    compile 'com.mcxiaoke.volley:library:1.0.+'
    // Facebook SDK
    compile project(':facebook')
    // Greendao generator. Generate the database object and structure.
    compile project(':generator')
    // Page Adapter for the application desing.
    compile project(':PagerSlidingTabStrip')
    // Firebase
    compile files('libs/firebase-client-jvm-LATEST.jar')
    compile files('libs/firebase-simple-login-LATEST.jar')
    // Parse
    compile files('libs/Parse-1.5.0.jar')
    // Greendao library for using the database. -
    // Has is own support library in it so need to exclude it so no TOP_LEVEL_EXCEPTION will occur.
    compile ('de.greenrobot:greendao:1.3.0'){ exclude group: 'com.google.android', module: 'support-v4'}
}